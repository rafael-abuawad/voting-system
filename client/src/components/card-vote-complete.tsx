import { Card, CardDescription, CardHeader, CardTitle } from "./ui/card";

export function CardVoteComplete() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Votar</CardTitle>
                <CardDescription>La votación ya termino.</CardDescription>
            </CardHeader>
        </Card>
    );
}
